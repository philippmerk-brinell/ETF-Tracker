# FRED API key — free at https://fredaccount.stlouisfed.org/login/secure/
FRED_API_KEY=your_fred_api_key_here

# Make.com webhook URL — required for WhatsApp notifications
# Create a webhook trigger in Make.com, connect it to a WhatsApp module
MAKE_WEBHOOK_URL=https://hook.eu1.make.com/xxxxxxxxxxxxxxxxxxxxxxxx

# Optional: override SQLite database path (default: data/signal_state.db)
# DB_PATH=data/signal_state.db
